/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";

export const img = css({
  width: "100%",
  opacity: "0.2",
});

export const imgText = css({
  zIndex: "100",
});

export const container = css({
});
